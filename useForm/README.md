# useForm Hook

Ejemplo de uso:

```
initialForm={
    name: '',
    edad: 0,
    email: ''
}
const [values, handleInputChange, reset] =useForm(initialForm);

```

3 backticks en md quiere decir código
